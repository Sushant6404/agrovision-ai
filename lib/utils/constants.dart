import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.green;
  static const secondary = Colors.teal;
}

class AppStrings {
  static const appName = "AgroVision AI 🌱";
}
